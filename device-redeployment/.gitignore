# Real config containing secrets — only the .example version is committed
config/network.yaml

# Python
__pycache__/
*.pyc

# Logs — keep the directory (via logs/.gitkeep) but not actual log output
logs/*.log
